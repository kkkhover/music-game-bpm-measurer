# BPM 测速助手 · 依赖库与实现原理

> 版本：**主程序 v0.8.15** / 内置侧栏 **v0.8.12**
> 本文分两部分：**① 依赖库清单**（含版本与许可证）、**② 关键功能的实现原理**。

---

# 一、依赖库清单

## 1.1 主程序（Electron + React + TypeScript）

**运行时依赖（真正随程序跑起来的）**

| 库 | 版本 | 许可证 | 用途 |
|---|---|---|---|
| `electron` | 34.5.8 | MIT | 桌面外壳：Chromium 渲染 + Node 主进程 |
| `react` / `react-dom` | 19.2.4 | MIT | 界面框架（被 Vite 打进 bundle） |
| `soundtouchjs` | 0.3.0 | **LGPL-2.1** | **变速不变调**播放（慢速听拍用） |
| 本地 `public/tailwind-play.js` | Tailwind Play **3.4.17** | MIT | 样式引擎，**随包发布**、不走 CDN |

**开发/构建期依赖（不随程序发布）**

| 库 | 版本 | 许可证 | 用途 |
|---|---|---|---|
| `typescript` | 5.8.3 | Apache-2.0 | 主进程 / preload / 前端类型检查与编译 |
| `vite` | 6.4.1 | MIT | 前端打包 |
| `@vitejs/plugin-react` | 5.2.0 | MIT | React 的 Vite 插件 |
| `electron-builder` | 25.1.8 | MIT | 打包成 NSIS 安装包 / 免安装目录 |
| `app-builder-bin` | 4.2.0 | MIT | electron-builder 的原生工具链 |
| `@electron/fuses` | 2.1.3 | MIT | Electron 二进制开关（安全加固） |
| `lucide-react` | 0.563.0 | ISC | 图标 |
| `concurrently` / `wait-on` | 9.2.1 / 8.0.5 | MIT | 开发期并行起 Vite + Electron |
| `@types/node` `@types/react` `@types/react-dom` | — | MIT | 类型声明 |

> ⚠️ **许可证注意**：`soundtouchjs` 是 **LGPL-2.1**。本项目**以依赖形式引用、未修改其源码**，
> 满足 LGPL 的动态链接使用条件；若将来要修改它，需按 LGPL 公开修改后的源码。

## 1.2 侧栏（osu-maphelper）

| 项 | 说明 |
|---|---|
| 运行时第三方依赖 | **零**。只用 Node 内置模块（`fs` / `path` / `http` / `zlib` / `child_process`）+ 浏览器原生 API（Web Audio、Canvas、fetch） |
| 开发期依赖 | 仅 `electron`（版本与主程序一致 34.5.8），**且不随侧栏源码分发** |
| Node 版本要求 | ≥ 20 |

> 侧栏之所以能"零依赖"，是因为它**不自己起 Electron** —— 发布时它被塞进主程序同一个 exe，
> 用 `--maphelper` 参数分流启动（见 2.1）。

## 1.3 外部程序（非本软件组件，但使用/参考了）

| 程序 | 关系 |
|---|---|
| **tosu** | 侧栏的**数据源**：通过本机 HTTP 拿 osu! 实时状态。需用户自行安装运行 |
| **Jakads/malody2osu**（`mcz转osz` / `convert.py`） | **参考实现**：本项目照它的算法来写 Malody 的 `beat`，保证转换后时间准确 |
| **Malody** | 目标音游；`.mc` / `.mcz` 格式以其实际行为为准 |

---

# 二、实现原理

## 2.1 双模式入口：一个 exe 既当主程序、又当侧栏

Electron 打包后，入口被写死成 `<安装目录>\resources\app`，命令行传别的目录会被忽略。
所以"同一个 exe 跑两个程序"**只能靠入口文件按启动参数分流**：

```
BPM 测速助手.exe               → 主程序（import dist-electron/main.js）
BPM 测速助手.exe --maphelper    → 内置侧栏（require maphelper/electron/main.cjs）
```

分流点是一个 **`.cjs`** 文件（`boot.cjs`）——因为主程序是 ESM（`"type": "module"`，`.js` 不能 `require`）、
侧栏是 CJS（`require` 最直接），而 **`.cjs` 里 `import()` 和 `require()` 都能用**，正好各取所需。

**`asar: false`** 是有意为之：侧栏要通过自定义协议按普通文件路径读取自己的 `renderer/`、`src/`，
打进 asar 反而麻烦；且关掉 asar 后用户/开发者排查问题更容易。

## 2.2 完全离线可用

主程序界面样式**全部**是 Tailwind 工具类。早期版本用了 `cdn.tailwindcss.com`，
**断网 → 脚本下不来 → 104 个带工具类的元素全部"裸奔"**（布局垮、颜色丢）。

现在改为把 Tailwind 官方 **Play 构建（运行时扫 DOM 生成样式，3.4.17，MIT）** 放进
`public/tailwind-play.js` 随包发布，`index.html` 用相对路径引用。行为与原来完全一致，只是不再需要网络。

同时删掉了 `index.html` 里那份指向 `esm.sh` 的 `importmap` —— Vite 早就把 React 打进 bundle 了，
那份是死代码，但会给"离线可用"埋雷。

## 2.3 音频：解码、播放、变速

| 环节 | 做法 |
|---|---|
| 解码 | `AudioContext.decodeAudioData()`（Chromium 内置解码器）→ `AudioBuffer`（**PCM 浮点**，内存占用 ≈ 时长 × 采样率 × 声道 × 4 字节） |
| 播放 | `AudioBufferSourceNode` → `GainNode` → destination；播放位置按 `ctx.currentTime` 换算 |
| 变速不变调 | `soundtouchjs`（SoundTouch 的 JS 移植），把播放速度降到 30%~50% 时**音高不变**，用于听清鼓点 |
| 波形 | 每 N 个采样取一段的**极小/极大值**降采样成 `Float32Array`（`extractPeaks`），Canvas 画成上下对称的包络 |

## 2.4 声谱 / 频谱：两条路径 + 一个缓存

频谱是本软件最吃性能也最容易出内存问题的地方，实现上有三个要点。

### (a) 实时频谱 —— `AnalyserNode`
播放时对当前音频取 `AnalyserNode`，每帧 `getByteFrequencyData` 拿一列频域数据直接画。
只是"当下这一瞬"，代价很低。

### (b) 整曲频谱 —— `OfflineAudioContext` 预计算（Chromium 原生 FFT）
要在时间轴上**任意滚动**都能立刻看到声谱，就必须**整曲提前算好**：

1. 建一个**整曲长度**的 `OfflineAudioContext`；
2. `BufferSource`（源 = 整曲音频）→ `AnalyserNode`（`fftSize` 由设置决定）→ `ScriptProcessor`；
3. `ScriptProcessor.onaudioprocess` 每块调 `getFloatFrequencyData`，把该列 dB 转成 `Uint8Array` 存起来
   （列步进 = `fftSize / 4`，即 75% 窗重叠，AU 风格）；
4. `startRendering()` 一次渲染到底，得到 `cols: Uint8Array[]` 供绘制直接查表。

失败/不支持时回退 **JS FFT**（`computeFFT`，Hann 窗 + 蝶形运算）—— 注意回退路径的幅值
**必须乘 `fftScale = 2/n`** 才能和原生 FFT 对齐量纲。

### (c) 两个必须遵守的约束（都是踩过坑写进去的）

- **用完必须显式拆掉离线音频图**。渲染完直接 `return` 会让整条图（连同该曲的整曲 PCM、整曲渲染缓冲）
  被钉住 → **每换一张谱面永久多占约 44MB**（10 分钟的歌约 300MB）。修法：`return` 之前
  `processor.onaudioprocess = null` → 逐个 `disconnect()` → **`src.buffer = null`**。
- **整曲预计算必须"串行 + 只保留最新"**。离线渲染**无法中途取消**（token 只能拦住"结果不被采用"，
  渲染本身照样跑完）。若每次换曲都无条件起一个新的，2 秒切一张、单次渲染 10 秒以上 →
  **每切一次就多一个渲染在跑**。实测快速连切 10 次时**并发峰值 10**、进程内存 +1580MB（真机峰值 **6.3GB**），
  而且**最后一张的频谱反而没算出来**（被挤在队尾）。
  → 加一个调度器：同一时刻只跑一个；在跑时只记下"最新那张"（后到覆盖先到），跑完再取最新。

绘制侧还有一层**列缓存**（`sampleIdx → 颜色列`），键**只含 `sampleIdx`**，
绝不能让缩放/宽度进签名（否则一缩放缓存全失效）；上限 3000 列，超出按插入顺序淘汰。

## 2.5 数据模型：红线的"权威值"是绝对时间

软件里每一段变速存的是：

| 字段 | 含义 | 是否权威 |
|---|---|---|
| `timeSec` | 该段开始的**绝对时间**（秒，浮点） | ★ **权威** |
| `bpm` | 该段的 BPM | ★ 权威 |
| `beatIndex` | 该段开始的**整拍编号**（供显示/对齐） | 派生值（取整） |

**为什么强调这点**：`beatIndex` 是取整出来的显示值，一旦拿它去算别的东西就会引入误差。
（v0.8.11 的 Malody 时间偏移 bug 就是"把取整的 `beatIndex` 写进 `.mc`"造成的，见 2.6。）

派生换算集中在 `utils/timingUtils.ts`：

- `recalculateTiming(offset, points)`：给定全局 offset，重算每一段的绝对时间与 `beatIndex`；
- `getPointAtTime` / `getBeatIndexAtTime` / `getTimeAtBeatIndex`：时间 ↔ 拍号 ↔ 段落的互查。

底部显示的"当前 BPM"= 播放头所在段落（`getPointAtTime`）的 BPM。

> 说明：本软件**不做自动 BPM 识别**，BPM 由使用者听 + 看波形/声谱手动确定并输入。
> 这与软件定位（"手动可调"）一致 —— 变速曲的自动识别不可靠，手动打点 + 可视化校验反而更快更准。

## 2.6 导出原理

### (a) osu! `[TimingPoints]`

| 行类型 | 判定 | 字段 |
|---|---|---|
| **红线**（BPM 变化） | `beatLength > 0` | `beatLength = 60000 / BPM`，`uninherited = 1` |
| **绿线**（变速 SV） | `beatLength < 0` | `beatLength = -100 / SV`，`uninherited = 0` |

**绿线倍速 SV = 基准BPM ÷ 当前BPM**（基准 = **时间最早的那条红线**的 BPM）。
这样 BPM 变快时 SV < 1（反向减速），BPM 变慢时 SV > 1，**下落流速保持恒定**。
不是无脑写 `1.0`。取两位小数（`roundHalfUp`），绿线字段 `time,<beatLength>,4,1,0,72,0,0`。

侧栏导出用 **merge 模式**：红线整体替换为内存里的编辑列表，**每条红线后跟一条绿线**。

### (b) Malody `.mc`

`.mc` 是纯 JSON（**不能带 `//` 注释头**，必须紧凑单行）。关键字段：

```json
{
  "meta": { "song": { "file": "<音频文件名>" }, "background": "", "$ver": 0 },
  "time":   [ { "beat": [a,b,c], "bpm": 182, "delay": 0.0 } ],   ← 红线
  "effect": [ { "beat": [a,b,c], "scroll": 1.0 } ],               ← 绿线（变速）
  "note":   [ … , { "beat":[0,0,1], "sound":"<音频文件名>", "vol":100, "offset": -3477, "type": 1 } ]
}
```

三条硬约束（全部来自 `mcz转osz` 的实际实现 + 真机验证）：

1. **`beat = [a,b,c]` 表示 `a + b/c` 拍**（`a` 是整拍号，`b/c` 是分数部分）。
   写侧要把**时间反算成累计拍位置**再写成 `a + b/1440`（1440 能被 Malody 用过的所有分母整除，
   精度约 0.4ms）。**绝不能直接写取整的 `beatIndex`** —— 转换器算红线时间是
   `t[i] = t[i-1] + (beat[i] - beat[i-1]) × 60000 / bpm[i-1]`（首条由 note 的 offset 决定），
   用整拍会**逐条累积误差**（实测最大偏 −121ms，听感就是"BPM 变化点不在拍上"）。
2. **`time[].delay` 转换器根本不读**，位置完全由 `beat` 差值决定 —— 所以第 1 条必须严格遵守。
3. **音频文件名只在 `note` 的最后一条（`type ≠ 0`）里指定**，且 Malody **只在该谱面文件夹里**
   按这个名字找音频。对不上时 Malody 会**静默退回约 1 秒的占位音源** → 表现为
   "**编辑器里音乐只有几秒钟**"。
   另外 **Malody 的谱面长度以最后一个音符为准**，所以还会把一个**占位音符放在音频末尾**，
   否则谱面只有 1.3 秒长。

### (c) `.mcz` 谱面包 = 自写的 ZIP

为了让"音频名对不上"从可能变成不可能，v0.8.12 起支持把 `.mc` + 音频一起打成 `.mcz`（拖进 Malody 即导入）。
这个 ZIP 是**主进程自己写的**，没有引入任何第三方压缩库：

- 表驱动 **CRC-32**；
- DOS 日期时间字段；
- 每个条目用 Node 内置 **`zlib.deflateRawSync`** 压缩（DEFLATE raw 正是 ZIP 条目的格式）；
- UTF-8 文件名（置 UTF-8 标志位）；
- 平铺结构（`.mc` 与音频直接在根目录），对齐真实 `.mcz`。

### (d) 其他格式

`utils/gameExports.ts` 里还有 Arcaea `.aff`（Timing 段）、Phigros（`BPMList`）、
ADOFAI（`SetSpeed` actions）、vivid/stasis（`vschart.json` 的 timingPoints）。
JSON 类一律**紧凑单行、无注释**；只有 Arcaea 的文本格式允许注释头。

## 2.7 侧栏的数据链路

```
osu!stable  ──读取内存──▶  tosu  ──HTTP GET /json/sc──▶  侧栏（只 fetch 这一个端点，100ms 一次）
                                                              │
                                          ┌───────────────────┴────────────────────┐
                                    当前谱面/曲名/音频路径                实时播放位置
                                          │
                                    打开磁盘上的 .osu（红线 timing 的权威来源）
```

- **侧栏不读 osu! 内存**，一切经由 tosu；tosu 没开侧栏就没有数据（这是设计如此，不是 bug）。
- **红线的权威来源是磁盘上的 `.osu`**；用户在侧栏里的编辑作为"内存编辑状态"存在**主进程**
  （`memTiming` / `memDirty`），导出时才落盘成新文件，**绝不写回原谱面**。
- 侧栏是**多窗口**：主窗 + `viz`/`map`/`backup`/`settings`/`log` 面板。窗口相关的 API 在
  `electron/main.cjs` 里**先于路由拦截**处理。

## 2.8 反节流与反降频（为什么切进游戏全屏也不掉帧）

Chromium 默认会"省电"：窗口不可见就降定时器频率、进入游戏全屏时还会把侧栏当成被遮挡的窗口冻结。
后果就是侧栏在游戏里**卡成幻灯片**。对策是多层的：

| 层 | 措施 |
|---|---|
| 每个窗口 | `backgroundThrottling: false`（**新增窗口必须记得加**） |
| 主进程开关 | `disable-background-timer-throttling`、`disable-renderer-backgrounding`、`disable-backgrounding-occluded-windows` |
| 特性开关 | `disable-features=CalculateNativeWinOcclusion,IntensiveWakeUpThrottling` |
| 系统层 | `powerSaveBlocker`（阻止系统降频/休眠）、提升进程优先级 |
| 兜底 | viz 里做 **rAF 失联心跳**：超 120ms 没有 rAF 回调就改由 `setInterval` 接管绘制 |

逃生口（排查时可关掉）：`OSU_MAPHELPER_NO_PRIORITY_BOOST`、`OSU_MAPHELPER_CANVAS_GPU`。

## 2.9 平滑跟随：为什么"卡顿"的真因不是帧率

tosu 给的位置数据是**跳变的**（约 100~150ms 一跳）。直接拿它画播放头，
无论渲染帧率多高，看起来都是"一顿一顿"。

→ 侧栏改为**本地外推**：以两次数据到达之间的时间自行推进播放头位置（`osuSmooth`），
只在数据更新时做校正。实测有效位置更新率从 ~10/s 提升到 151~168/s，肉眼即连续。

另有**可调兜底帧率**：rAF 被降频时，按设定值用定时器补帧。

## 2.10 读取另一个 Electron 程序存在磁盘上的设置

侧栏需要复用主程序（BPM 测速助手）的某些设置，于是直接读对方的
`%APPDATA%\<package.json 的 name>\Local Storage\leveldb\`。两个坑：

- **`.log` 是只追加的**：同一个键会留下**历次写入的全部历史值**，只有**最后一条**才是当前值。
  用 `indexOf` 取"第一次出现"会读到**最旧**的值 → 必须**全量扫描、后面的覆盖前面的**，
  且按文件 mtime 取最新那个文件。
- **编码**：值全是 ASCII 时 Chromium 存 **Latin-1**；含非 Latin-1 字符才转 **UTF-16LE**。
  两种都试一遍最稳；抠 JSON 用**括号配平**而不是正则（嵌套对象会切错）。
- 读不到时**优雅降级**（退回自己的值 + 界面上说明原因），绝不静默用错值。

## 2.11 内存：本软件的四条经验（都来自实测）

判据：**必须用 OS 级 Private Bytes（`PagefileUsage`）**。用 `WorkingSet` 会被系统回收干扰；
用 JS 堆更不行 —— **ArrayBuffer 的存储属于 V8 external memory，不在堆统计里**，
而音频缓冲、频谱列数据恰恰都在那儿。

| # | 结论 |
|---|---|
| 1 | 离线音频图**用完要拆**（否则每张谱面永久 +44MB） |
| 2 | 整曲预计算**必须串行 + 只保留最新**（否则快速连切并发叠到 10、峰值 6.3GB） |
| 3 | 频谱面板渲染进程**本身固定占几百 MB ~ 1GB**：整曲 × 缩放 × 渲染倍率 的画布层，**不是泄漏**、不随换谱面增长。想省：降渲染倍率 / `fftSize` 512 / 调大轮询间隔 |
| 4 | **`tosu.exe` 自身的泄漏**（+2.5~3.1 GB/小时）与切谱面**无因果关系**；本软件未做规避 |

---

## 附：一句话技术栈总结

> **主程序**：Electron 34 + React 19 + TypeScript 5.8 + Vite 6，样式 Tailwind（本地内置），
> 音频 Web Audio API + soundtouchjs，打包 electron-builder/NSIS，**完全离线可用**。
> **侧栏**：纯 Node + 浏览器原生 API，**零第三方运行时依赖**；数据走 tosu HTTP，
> 频谱走 Chromium 原生 FFT（整曲离线预计算），用一个 exe 靠 `--maphelper` 分流启动。
